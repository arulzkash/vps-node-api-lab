services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-api:latest
    container_name: my-api-docker
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
