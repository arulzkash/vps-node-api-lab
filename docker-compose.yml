services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-api:latest
    container_name: my-api-docker
    restart: unless-stopped
    ports:
      - "127.0.0.1:${HOST_PORT}:${PORT}"
    environment:
      NODE_ENV: ${NODE_ENV}
      HOST: ${HOST}
      PORT: ${PORT}
